from random import randint


# We will write 2 functions:
# 1 -> The classifier which takes an m*n matrix. 
#     m -> number of entries
#     n-1 -> features
#     1 -> label(0 or 1)

#     returns a decision tree which is a matrix
# 2 -> The result which takes in two matrix. A decision tree and a question matrix of order 1*(n-1) and returns a label 0 or 1